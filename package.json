{
  "name": "volt-consumo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:static": "python tests/quality_gate.py",
    "test:shake": "node tests/shake-detector.test.mjs",
    "test:browser": "playwright test",
    "test": "npm run check:static && npm run test:shake && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.54.2"
  }
}
